import { NgbdListSortableHeaderDirective } from './ngbd-list-sortable-header.directive';

describe('NgbdListSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbdListSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
